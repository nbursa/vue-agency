@import "../colors"

.modal-background {
  width:100%;
  height:100vh;
  z-index:9;
  background-color:rgba(0,0,0,0.4);
  position:absolute;
  top:0;
  transition: opacity .3s ease;
}

.modal-enter {
  opacity: 0;
}

.modal-leave-active {
  opacity: 0;
}