export const UPDATE_STATUS = (state, status) => state.status = status

export const UPDATE_BONUSES = (state, bonuses) => state.bonuses = bonuses

export const UPDATE_META_DATA = (state, meta) => state.meta = meta

export const SET_ERRORS = (state, errors) => state.errors = errors