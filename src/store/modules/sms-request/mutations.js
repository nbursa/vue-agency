export const SET_JOB_REQUEST = (state, jobRequest) => state.jobRequest = jobRequest

export const SET_ERRORS = (state, errors) => state.errors = errors

export const SET_TOKEN = (state, token) => state.token = token

export const SET_EMAIL = (state, email) => state.email = email
